# Record 20260817-011745-7d9130d

- **Record ID**: 20260817-011745-7d9130d
- **Claim**: A gate-level netlist for `tmds_encoder` exists, timing-driven-synthesized
  (issue #100, follow-up to #83's STA finding that the prior area-only netlist failed
  setup at 4/5 corners) against the gf180mcu standard-cell library
  (`gf180mcu_fd_sc_mcu9t5v0`), within the 3.3 V corner family spec/tmds-tx.md DR-0003 names for
  the synthesized domain. Closes #65 item 1 (design sources) on the digital partition.
- **Scope**: Synthesis only, now timing-driven (issue #100 step 1 of 3 -- see
  `flow/synth_tmds_encoder.py`'s "Timing-driven mapping" docstring section). No
  standalone STA/timing-closure claim is made by *this* record -- that is
  `flow/sta_tmds_encoder.py`'s job, re-run separately after this netlist and its
  place-and-route.
- **Tool versions**:
  - Yosys: `Yosys 0.68+post (git sha1 c12172fbae8af5e20f6fb52e3d4e92d56ed587b6, Release, AppleClang clang++ 21.0.0.21000101)`
  - gf180mcu PDK: variant `gf180mcuD`, open_pdks `c6d73a35f524070e85faff4a6a9eef49553ebc2b` (via search_root:~/.volare)
- **Standard-cell library**: `libs.ref/gf180mcu_fd_sc_mcu9t5v0/lib/gf180mcu_fd_sc_mcu9t5v0__tt_025C_3v30.lib` (`gf180mcu_fd_sc_mcu9t5v0`,
  `tt_025C_3v30` nominal corner -- reporting/provenance only, see below)
- **Timing-driven mapping** (issue #100):
  - ABC delay target: `-D 13468` (13.4680 ns = 1/74.25 MHz,
    `spec/tmds-tx.md` §2's 720p60 pixel clock -- not invented here).
  - Mapping corner: `libs.ref/gf180mcu_fd_sc_mcu9t5v0/lib/gf180mcu_fd_sc_mcu9t5v0__ss_125C_3v00.lib` (`ss_125C_3v00`) --
    the **worst setup corner** per issue #83's STA record `20260816-172539-930e864`,
    not the `tt_025C_3v30` nominal corner DR-0003 names for the synthesized
    domain generally. Synthesizing against the slowest corner's own cell-delay arcs
    means a netlist meeting the `-D` target there carries positive margin at every
    faster corner by construction -- see this module's docstring for the full
    rationale for why this is not a DR-0003 deviation.
  - I/O boundary (`-constr`, exact file: `flow/tmds_encoder/reports/20260817-011745-7d9130d.synth.constr`):
    `set_driving_cell gf180mcu_fd_sc_mcu9t5v0__inv_1` / `set_load 27.252` (femtofarads)
    -- numerically identical to `flow/sta_tmds_encoder.py`'s own
    `DRIVING_CELL`/`OUTPUT_LOAD_PF` assumption, so synthesis cannot assume a tighter
    boundary than STA later checks against.
- **Synthesis constraints**: technology mapping plus the timing-driven target above.
  Recipe: `proc; opt; fsm; opt; memory; opt; techmap; opt; dfflibmap -liberty <timing
  lib>; abc -liberty <timing lib> -constr <constr file> -script <abc script>;
  opt_clean -purge; clean`. The `-script` is a custom recipe (`build_abc_script` in
  `flow/synth_tmds_encoder.py`) that replicates Yosys's own default `-D`-driven
  `-constr` script **except** it omits the `retime -o -D <ps>` splice that bare
  `-D` would otherwise insert -- that splice was found, directly, to corrupt this
  design (`tmds` primary output left with no driver at all); see that function's
  docstring for the full finding. Exact scripts:
  `flow/tmds_encoder/reports/20260817-011745-7d9130d.synth.ys` (Yosys),
  `flow/tmds_encoder/reports/20260817-011745-7d9130d.synth.abc` (ABC), and the `-constr` file above.
- **Result**: PASS -- 291 cell instances (29 sequential, 262
  combinational), 0 unmapped cells (every instance is a `gf180mcu_fd_sc_mcu9t5v0__*` standard
  cell; checked both by the synthesis script's own
  `select -assert-none t:$_*` and by re-parsing the written netlist -- see
  `cell_counts`/`assert_fully_mapped` in `flow/synth_tmds_encoder.py`). Cell
  breakdown:
  - `gf180mcu_fd_sc_mcu9t5v0__and2_1`: 13
  - `gf180mcu_fd_sc_mcu9t5v0__and3_1`: 2
  - `gf180mcu_fd_sc_mcu9t5v0__aoi211_1`: 6
  - `gf180mcu_fd_sc_mcu9t5v0__aoi21_1`: 24
  - `gf180mcu_fd_sc_mcu9t5v0__aoi221_2`: 1
  - `gf180mcu_fd_sc_mcu9t5v0__aoi22_1`: 5
  - `gf180mcu_fd_sc_mcu9t5v0__aoi22_4`: 1
  - `gf180mcu_fd_sc_mcu9t5v0__buf_1`: 3
  - `gf180mcu_fd_sc_mcu9t5v0__buf_2`: 1
  - `gf180mcu_fd_sc_mcu9t5v0__clkinv_1`: 15
  - `gf180mcu_fd_sc_mcu9t5v0__dffq_1`: 29
  - `gf180mcu_fd_sc_mcu9t5v0__mux2_1`: 1
  - `gf180mcu_fd_sc_mcu9t5v0__nand2_1`: 49
  - `gf180mcu_fd_sc_mcu9t5v0__nand3_1`: 6
  - `gf180mcu_fd_sc_mcu9t5v0__nand4_1`: 3
  - `gf180mcu_fd_sc_mcu9t5v0__nor2_1`: 22
  - `gf180mcu_fd_sc_mcu9t5v0__nor2_2`: 2
  - `gf180mcu_fd_sc_mcu9t5v0__nor2_4`: 2
  - `gf180mcu_fd_sc_mcu9t5v0__nor3_1`: 2
  - `gf180mcu_fd_sc_mcu9t5v0__nor3_2`: 2
  - `gf180mcu_fd_sc_mcu9t5v0__nor3_4`: 1
  - `gf180mcu_fd_sc_mcu9t5v0__nor4_1`: 2
  - `gf180mcu_fd_sc_mcu9t5v0__oai211_1`: 6
  - `gf180mcu_fd_sc_mcu9t5v0__oai21_1`: 13
  - `gf180mcu_fd_sc_mcu9t5v0__oai21_2`: 1
  - `gf180mcu_fd_sc_mcu9t5v0__oai21_4`: 1
  - `gf180mcu_fd_sc_mcu9t5v0__oai22_1`: 2
  - `gf180mcu_fd_sc_mcu9t5v0__oai32_1`: 2
  - `gf180mcu_fd_sc_mcu9t5v0__oai32_4`: 1
  - `gf180mcu_fd_sc_mcu9t5v0__or2_1`: 7
  - `gf180mcu_fd_sc_mcu9t5v0__or2_2`: 2
  - `gf180mcu_fd_sc_mcu9t5v0__or2_4`: 2
  - `gf180mcu_fd_sc_mcu9t5v0__or4_1`: 1
  - `gf180mcu_fd_sc_mcu9t5v0__xnor2_1`: 26
  - `gf180mcu_fd_sc_mcu9t5v0__xnor2_2`: 2
  - `gf180mcu_fd_sc_mcu9t5v0__xnor2_4`: 1
  - `gf180mcu_fd_sc_mcu9t5v0__xnor3_1`: 7
  - `gf180mcu_fd_sc_mcu9t5v0__xnor3_2`: 2
  - `gf180mcu_fd_sc_mcu9t5v0__xnor3_4`: 1
  - `gf180mcu_fd_sc_mcu9t5v0__xor2_1`: 17
  - `gf180mcu_fd_sc_mcu9t5v0__xor2_2`: 1
  - `gf180mcu_fd_sc_mcu9t5v0__xor2_4`: 2
  - `gf180mcu_fd_sc_mcu9t5v0__xor3_1`: 1
  - `gf180mcu_fd_sc_mcu9t5v0__xor3_2`: 1
- **Warnings**: every distinct `Warning:` line the run's log contains, explained rather
  than left as noise (per issue #82's "no synthesis warnings left unexplained"
  acceptance criterion; note the captured log can end early on this repo's
  `yowasp-yosys` install after ABC's liberty-load stage even though the run itself
  completes successfully -- see `flow/README.md`'s "yowasp-yosys" gotcha -- so this
  list is a lower bound, not a guarantee no further warning was printed):
  - `Warning: Wire tmds_encoder.\tmds [9] is used but has no driver.` -- A false positive from Yosys's `check` pass, not a real missing driver (issue #100 -- investigated directly, not assumed): `dfflibmap` maps each `$dff` to a named `gf180mcu_fd_sc_mcu9t5v0__dff*` liberty cell by name only, without importing a blackbox module that declares that cell's port directions, so `check`'s conservative used-vs-driven classifier cannot confirm the cell's `Q` port is an output at the point it runs and flags every wire solely driven by one as 'used but has no driver'. Confirmed benign by dumping the design's RTLIL immediately before `check` runs and by inspecting the final `write_verilog` output: every flagged bit (`tmds[0..9]`) is a normal `.Q(<net>)` cell-port connection from a real `gf180mcu_fd_sc_mcu9t5v0__dffq_1`/`dffrsnq_*` instance, with no `assign` or floating net involved. Reproduced identically (same 10 warnings) by re-running the *unmodified*, pre-#100 area-only recipe with no `-D`/`-constr`/timing-corner change at all -- this is a pre-existing Yosys diagnostic quirk, not something issue #100's timing-driven mapping introduced. It was invisible in #82's original evidence record only because that record's `yowasp-yosys` log truncates before reaching the `check` pass (a known, already-disclosed gotcha -- see `flow/README.md`'s 'yowasp-yosys' note); this driver's native-Yosys runs capture the full log and so are the first to actually see it.
  - `Warning: Wire tmds_encoder.\tmds [8] is used but has no driver.` -- A false positive from Yosys's `check` pass, not a real missing driver (issue #100 -- investigated directly, not assumed): `dfflibmap` maps each `$dff` to a named `gf180mcu_fd_sc_mcu9t5v0__dff*` liberty cell by name only, without importing a blackbox module that declares that cell's port directions, so `check`'s conservative used-vs-driven classifier cannot confirm the cell's `Q` port is an output at the point it runs and flags every wire solely driven by one as 'used but has no driver'. Confirmed benign by dumping the design's RTLIL immediately before `check` runs and by inspecting the final `write_verilog` output: every flagged bit (`tmds[0..9]`) is a normal `.Q(<net>)` cell-port connection from a real `gf180mcu_fd_sc_mcu9t5v0__dffq_1`/`dffrsnq_*` instance, with no `assign` or floating net involved. Reproduced identically (same 10 warnings) by re-running the *unmodified*, pre-#100 area-only recipe with no `-D`/`-constr`/timing-corner change at all -- this is a pre-existing Yosys diagnostic quirk, not something issue #100's timing-driven mapping introduced. It was invisible in #82's original evidence record only because that record's `yowasp-yosys` log truncates before reaching the `check` pass (a known, already-disclosed gotcha -- see `flow/README.md`'s 'yowasp-yosys' note); this driver's native-Yosys runs capture the full log and so are the first to actually see it.
  - `Warning: Wire tmds_encoder.\tmds [7] is used but has no driver.` -- A false positive from Yosys's `check` pass, not a real missing driver (issue #100 -- investigated directly, not assumed): `dfflibmap` maps each `$dff` to a named `gf180mcu_fd_sc_mcu9t5v0__dff*` liberty cell by name only, without importing a blackbox module that declares that cell's port directions, so `check`'s conservative used-vs-driven classifier cannot confirm the cell's `Q` port is an output at the point it runs and flags every wire solely driven by one as 'used but has no driver'. Confirmed benign by dumping the design's RTLIL immediately before `check` runs and by inspecting the final `write_verilog` output: every flagged bit (`tmds[0..9]`) is a normal `.Q(<net>)` cell-port connection from a real `gf180mcu_fd_sc_mcu9t5v0__dffq_1`/`dffrsnq_*` instance, with no `assign` or floating net involved. Reproduced identically (same 10 warnings) by re-running the *unmodified*, pre-#100 area-only recipe with no `-D`/`-constr`/timing-corner change at all -- this is a pre-existing Yosys diagnostic quirk, not something issue #100's timing-driven mapping introduced. It was invisible in #82's original evidence record only because that record's `yowasp-yosys` log truncates before reaching the `check` pass (a known, already-disclosed gotcha -- see `flow/README.md`'s 'yowasp-yosys' note); this driver's native-Yosys runs capture the full log and so are the first to actually see it.
  - `Warning: Wire tmds_encoder.\tmds [6] is used but has no driver.` -- A false positive from Yosys's `check` pass, not a real missing driver (issue #100 -- investigated directly, not assumed): `dfflibmap` maps each `$dff` to a named `gf180mcu_fd_sc_mcu9t5v0__dff*` liberty cell by name only, without importing a blackbox module that declares that cell's port directions, so `check`'s conservative used-vs-driven classifier cannot confirm the cell's `Q` port is an output at the point it runs and flags every wire solely driven by one as 'used but has no driver'. Confirmed benign by dumping the design's RTLIL immediately before `check` runs and by inspecting the final `write_verilog` output: every flagged bit (`tmds[0..9]`) is a normal `.Q(<net>)` cell-port connection from a real `gf180mcu_fd_sc_mcu9t5v0__dffq_1`/`dffrsnq_*` instance, with no `assign` or floating net involved. Reproduced identically (same 10 warnings) by re-running the *unmodified*, pre-#100 area-only recipe with no `-D`/`-constr`/timing-corner change at all -- this is a pre-existing Yosys diagnostic quirk, not something issue #100's timing-driven mapping introduced. It was invisible in #82's original evidence record only because that record's `yowasp-yosys` log truncates before reaching the `check` pass (a known, already-disclosed gotcha -- see `flow/README.md`'s 'yowasp-yosys' note); this driver's native-Yosys runs capture the full log and so are the first to actually see it.
  - `Warning: Wire tmds_encoder.\tmds [5] is used but has no driver.` -- A false positive from Yosys's `check` pass, not a real missing driver (issue #100 -- investigated directly, not assumed): `dfflibmap` maps each `$dff` to a named `gf180mcu_fd_sc_mcu9t5v0__dff*` liberty cell by name only, without importing a blackbox module that declares that cell's port directions, so `check`'s conservative used-vs-driven classifier cannot confirm the cell's `Q` port is an output at the point it runs and flags every wire solely driven by one as 'used but has no driver'. Confirmed benign by dumping the design's RTLIL immediately before `check` runs and by inspecting the final `write_verilog` output: every flagged bit (`tmds[0..9]`) is a normal `.Q(<net>)` cell-port connection from a real `gf180mcu_fd_sc_mcu9t5v0__dffq_1`/`dffrsnq_*` instance, with no `assign` or floating net involved. Reproduced identically (same 10 warnings) by re-running the *unmodified*, pre-#100 area-only recipe with no `-D`/`-constr`/timing-corner change at all -- this is a pre-existing Yosys diagnostic quirk, not something issue #100's timing-driven mapping introduced. It was invisible in #82's original evidence record only because that record's `yowasp-yosys` log truncates before reaching the `check` pass (a known, already-disclosed gotcha -- see `flow/README.md`'s 'yowasp-yosys' note); this driver's native-Yosys runs capture the full log and so are the first to actually see it.
  - `Warning: Wire tmds_encoder.\tmds [4] is used but has no driver.` -- A false positive from Yosys's `check` pass, not a real missing driver (issue #100 -- investigated directly, not assumed): `dfflibmap` maps each `$dff` to a named `gf180mcu_fd_sc_mcu9t5v0__dff*` liberty cell by name only, without importing a blackbox module that declares that cell's port directions, so `check`'s conservative used-vs-driven classifier cannot confirm the cell's `Q` port is an output at the point it runs and flags every wire solely driven by one as 'used but has no driver'. Confirmed benign by dumping the design's RTLIL immediately before `check` runs and by inspecting the final `write_verilog` output: every flagged bit (`tmds[0..9]`) is a normal `.Q(<net>)` cell-port connection from a real `gf180mcu_fd_sc_mcu9t5v0__dffq_1`/`dffrsnq_*` instance, with no `assign` or floating net involved. Reproduced identically (same 10 warnings) by re-running the *unmodified*, pre-#100 area-only recipe with no `-D`/`-constr`/timing-corner change at all -- this is a pre-existing Yosys diagnostic quirk, not something issue #100's timing-driven mapping introduced. It was invisible in #82's original evidence record only because that record's `yowasp-yosys` log truncates before reaching the `check` pass (a known, already-disclosed gotcha -- see `flow/README.md`'s 'yowasp-yosys' note); this driver's native-Yosys runs capture the full log and so are the first to actually see it.
  - `Warning: Wire tmds_encoder.\tmds [3] is used but has no driver.` -- A false positive from Yosys's `check` pass, not a real missing driver (issue #100 -- investigated directly, not assumed): `dfflibmap` maps each `$dff` to a named `gf180mcu_fd_sc_mcu9t5v0__dff*` liberty cell by name only, without importing a blackbox module that declares that cell's port directions, so `check`'s conservative used-vs-driven classifier cannot confirm the cell's `Q` port is an output at the point it runs and flags every wire solely driven by one as 'used but has no driver'. Confirmed benign by dumping the design's RTLIL immediately before `check` runs and by inspecting the final `write_verilog` output: every flagged bit (`tmds[0..9]`) is a normal `.Q(<net>)` cell-port connection from a real `gf180mcu_fd_sc_mcu9t5v0__dffq_1`/`dffrsnq_*` instance, with no `assign` or floating net involved. Reproduced identically (same 10 warnings) by re-running the *unmodified*, pre-#100 area-only recipe with no `-D`/`-constr`/timing-corner change at all -- this is a pre-existing Yosys diagnostic quirk, not something issue #100's timing-driven mapping introduced. It was invisible in #82's original evidence record only because that record's `yowasp-yosys` log truncates before reaching the `check` pass (a known, already-disclosed gotcha -- see `flow/README.md`'s 'yowasp-yosys' note); this driver's native-Yosys runs capture the full log and so are the first to actually see it.
  - `Warning: Wire tmds_encoder.\tmds [2] is used but has no driver.` -- A false positive from Yosys's `check` pass, not a real missing driver (issue #100 -- investigated directly, not assumed): `dfflibmap` maps each `$dff` to a named `gf180mcu_fd_sc_mcu9t5v0__dff*` liberty cell by name only, without importing a blackbox module that declares that cell's port directions, so `check`'s conservative used-vs-driven classifier cannot confirm the cell's `Q` port is an output at the point it runs and flags every wire solely driven by one as 'used but has no driver'. Confirmed benign by dumping the design's RTLIL immediately before `check` runs and by inspecting the final `write_verilog` output: every flagged bit (`tmds[0..9]`) is a normal `.Q(<net>)` cell-port connection from a real `gf180mcu_fd_sc_mcu9t5v0__dffq_1`/`dffrsnq_*` instance, with no `assign` or floating net involved. Reproduced identically (same 10 warnings) by re-running the *unmodified*, pre-#100 area-only recipe with no `-D`/`-constr`/timing-corner change at all -- this is a pre-existing Yosys diagnostic quirk, not something issue #100's timing-driven mapping introduced. It was invisible in #82's original evidence record only because that record's `yowasp-yosys` log truncates before reaching the `check` pass (a known, already-disclosed gotcha -- see `flow/README.md`'s 'yowasp-yosys' note); this driver's native-Yosys runs capture the full log and so are the first to actually see it.
  - `Warning: Wire tmds_encoder.\tmds [1] is used but has no driver.` -- A false positive from Yosys's `check` pass, not a real missing driver (issue #100 -- investigated directly, not assumed): `dfflibmap` maps each `$dff` to a named `gf180mcu_fd_sc_mcu9t5v0__dff*` liberty cell by name only, without importing a blackbox module that declares that cell's port directions, so `check`'s conservative used-vs-driven classifier cannot confirm the cell's `Q` port is an output at the point it runs and flags every wire solely driven by one as 'used but has no driver'. Confirmed benign by dumping the design's RTLIL immediately before `check` runs and by inspecting the final `write_verilog` output: every flagged bit (`tmds[0..9]`) is a normal `.Q(<net>)` cell-port connection from a real `gf180mcu_fd_sc_mcu9t5v0__dffq_1`/`dffrsnq_*` instance, with no `assign` or floating net involved. Reproduced identically (same 10 warnings) by re-running the *unmodified*, pre-#100 area-only recipe with no `-D`/`-constr`/timing-corner change at all -- this is a pre-existing Yosys diagnostic quirk, not something issue #100's timing-driven mapping introduced. It was invisible in #82's original evidence record only because that record's `yowasp-yosys` log truncates before reaching the `check` pass (a known, already-disclosed gotcha -- see `flow/README.md`'s 'yowasp-yosys' note); this driver's native-Yosys runs capture the full log and so are the first to actually see it.
  - `Warning: Wire tmds_encoder.\tmds [0] is used but has no driver.` -- A false positive from Yosys's `check` pass, not a real missing driver (issue #100 -- investigated directly, not assumed): `dfflibmap` maps each `$dff` to a named `gf180mcu_fd_sc_mcu9t5v0__dff*` liberty cell by name only, without importing a blackbox module that declares that cell's port directions, so `check`'s conservative used-vs-driven classifier cannot confirm the cell's `Q` port is an output at the point it runs and flags every wire solely driven by one as 'used but has no driver'. Confirmed benign by dumping the design's RTLIL immediately before `check` runs and by inspecting the final `write_verilog` output: every flagged bit (`tmds[0..9]`) is a normal `.Q(<net>)` cell-port connection from a real `gf180mcu_fd_sc_mcu9t5v0__dffq_1`/`dffrsnq_*` instance, with no `assign` or floating net involved. Reproduced identically (same 10 warnings) by re-running the *unmodified*, pre-#100 area-only recipe with no `-D`/`-constr`/timing-corner change at all -- this is a pre-existing Yosys diagnostic quirk, not something issue #100's timing-driven mapping introduced. It was invisible in #82's original evidence record only because that record's `yowasp-yosys` log truncates before reaching the `check` pass (a known, already-disclosed gotcha -- see `flow/README.md`'s 'yowasp-yosys' note); this driver's native-Yosys runs capture the full log and so are the first to actually see it.
- **Reproducibility**: working tree DIRTY (uncommitted changes outside flow/tmds_encoder/ at run time -- re-run against a clean checkout before trusting this record) at commit `7d9130de094d567a5c76b65869efda18ae302809`.
- **Links**:
  - RTL source: `rtl/tmds_encoder.v`
  - Netlist: `flow/tmds_encoder/netlist/tmds_encoder.synth.v`
  - Yosys script: `flow/tmds_encoder/reports/20260817-011745-7d9130d.synth.ys`
  - ABC script: `flow/tmds_encoder/reports/20260817-011745-7d9130d.synth.abc`
  - ABC `-constr` file: `flow/tmds_encoder/reports/20260817-011745-7d9130d.synth.constr`
  - Full log: `flow/tmds_encoder/reports/20260817-011745-7d9130d.synth.log`
- **Timestamp / author**: 2026-08-17 01:17:45 UTC, `flow/synth_tmds_encoder.py` (agent-run)
